reaction(
    in(sense(temp(T))),
    (operation, invocation),
    (
        sensor@localhost:20504 ? getEnv(temp, T)
    )
).
reaction(
    getEnv(temp, T),
    (from_env, completion),
    (
        out(sense(temp(T)))
    )
).