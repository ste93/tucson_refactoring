reaction(
    out(act(temp(T))),
    (operation, completion),
    (
        actuator@localhost:20504 ? setEnv(temp, T)
    )
).
reaction(
    setEnv(temp, T),
    (from_env, completion),
    (
        in(act(temp(T)))
    )
).