reaction(
    out(act(temp(T))),
    (operation, completion),
    (
        actuator@localhost:20504 ? setEnv(temp, T)
    )
).
reaction(
    setEnv(temp, T),
%    getEnv(temp, T),
    (from_env),
    (
        in(act(temp(T)))
    )
).