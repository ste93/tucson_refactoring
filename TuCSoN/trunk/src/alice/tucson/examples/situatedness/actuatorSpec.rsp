% When requested to stimulate the actuator to change the enviroment, do so
% exploiting ReSpecT situatedness.
reaction(
    out(act(temp(T))),
    (operation, completion),
    (
        actuator@localhost:20504 ? set_env(temp, T)
    )
).
% Delete garbage tuple.
reaction(
    set_env(temp, T),
    (from_env, completion),
    (
        in(act(temp(T)))
    )
).