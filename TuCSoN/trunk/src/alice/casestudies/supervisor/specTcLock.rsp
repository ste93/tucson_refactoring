
% ReSpecT specification used into tc_lock ( LIGHT SUPERVISOR CASE STUDY )

% Reacting to environment event
reaction(
	getEnv(lock,0),
	(from_env, request),
	(
		tc_intensity@localhost:20504 ? out(data(input(tc_ph))),
		tc_intensity@localhost:20504 ? in(input(tc_ps))
	)
).

reaction(
	getEnv(lock,1),
	(from_env, request),
	(
		tc_intensity@localhost:20504 ? in(input(tc_ph)),
		tc_intensity@localhost:20504 ? out(data(input(tc_ps)))
	)
).
	